#!/usr/bin/env sh

# Husky initialization script
# This file is loaded by Husky before running git hooks

# Load NVM if available
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" 